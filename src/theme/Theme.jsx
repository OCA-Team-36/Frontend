import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#E81255"
    },
    background: {
      default: "#F3F3F3",
      netral: "#FFFFFF",
    },
  },
});

export default theme;
